import DeckShell from "@/components/Deck/DeckShell";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Brand Partnerships & Sponsorships | Mall of America",
  description:
    "Build something bigger together. A Mall of America partnership is more than a deal — it's access to 40 million people, 30 years of brand trust, and the most coveted real estate in retail.",
};

export default function PartnershipPage() {
  return <DeckShell />;
}
