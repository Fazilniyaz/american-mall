import DeckShell from "@/components/Deck/DeckShell";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Become a Sponsor — Own Your Audience | Mall of America",
  description:
    "Sponsoring Mall of America means your brand lives where 40 million people spend their time. Presenting, Premium, and Associate tiers available.",
};

export default function BecomeASponsorPage() {
  return <DeckShell />;
}
